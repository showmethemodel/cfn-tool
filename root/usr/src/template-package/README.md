```
USAGE:
    template-package [OPTIONS...] <template-file>

OPTIONS:
    -h, --help
    -j, --json
    -t, --temp-dir=DIR
    -b, --s3-bucket=BUCKET
    -p, --s3-prefix=PREFIX
    -o, --out-file=FILE
```
